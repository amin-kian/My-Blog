# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies
# ----------------------------------------------------------------
# The node_modules directory contains all of your project's dependencies.
# It can be very large and should always be regenerated by running `npm install` or `yarn install`.
/node_modules

# Next.js build output
# ----------------------------------------------------------------
# The .next directory is the build cache and output. It's generated when you run `next dev` or `next build`.
/.next/

# Production build
# ----------------------------------------------------------------
# The out directory is generated when you run `next export`.
/out/

# Environment variables
# ----------------------------------------------------------------
# Never commit your environment variables. These files contain sensitive information
# like database passwords and API keys. The `.env.local` file is the standard for local secrets.
.env.local
.env.development.local
.env.production.local
.env.test.local
.env

# Log files
# ----------------------------------------------------------------
# Log files can become large and are specific to your local environment.
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor directories and files
# ----------------------------------------------------------------
# These files are specific to your code editor (VSCode, WebStorm, etc.) and
# should not be shared with other collaborators.
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Operating System files
# ----------------------------------------------------------------
# These are files generated by your operating system (macOS, Windows).
.DS_Store
Thumbs.db

# Test outputs
# ----------------------------------------------------------------
# Coverage reports and other test-related outputs.
/coverage
